#pragma once
#include "cuda_runtime.h"
#include "device_launch_parameters.h"
#include <conio.h>
#include <stdio.h>
#include "CUDA.cuh"

int main()
{
	elog;
	_logtxt.Clearlog();
	EMATRIX A(100, 100);
	EMATRIX B(100, 1000);
	A = ones();
	for (int i = 0; i < 10000; i++)
		B.vectors[i] = i;    
	clock_t t;
	t = clock();
	EMATRIX* C = CUDA_MATRIX_CONVERT(&A, &B, MULTI);
	t = clock() - t;
	double time_taken = ((double)t) / CLOCKS_PER_SEC;
	cout <<"TIME = " + to_string(time_taken) + "\n";
	cout << "END" << endl;
	return 0;
}
